<Window x:Class="Ofcas.Lk.Api.Client.Demo.Views.SelectValueWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:utils="clr-namespace:Ofcas.Lk.Api.Client.Demo.Utils"
        Title="{Binding Title.Value}"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        utils:DialogCloser.DialogResult="{Binding DialogResult.Value}"
        mc:Ignorable="d">
    <Grid>
        <Grid Height="25"
              Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="80" />
            </Grid.ColumnDefinitions>

            <ComboBox Grid.Column="2"
                      utils:KeyboardFocusManager.FocusOnStartup="True"
                      ItemsSource="{Binding Values}"
                      SelectedItem="{Binding Selected.Value}" />
            
            <Button Grid.Column="4"
                    Command="{Binding AcceptCommand}"
                    Content="OK"
                    IsDefault="True" />
        </Grid>
    </Grid>
</Window>
