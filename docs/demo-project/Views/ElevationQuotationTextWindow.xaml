<views:BaseWindow x:Class="Ofcas.Lk.Api.Client.Demo.Views.ElevationQuotationTextWindow"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                  xmlns:views="clr-namespace:Ofcas.Lk.Api.Client.Demo.Views"
                  xmlns:viewModels="clr-namespace:Ofcas.Lk.Api.Client.Demo.ViewModels"
                  mc:Ignorable="d"
                  Title="{Binding ElevationName, StringFormat='{}{0} - Quotations Texts'}"
                  WindowStartupLocation="CenterScreen"
                  Height="500"
                  Width="600"
                  d:DataContext="{d:DesignInstance viewModels:ElevationQuotationTextsViewModel}">

    <DockPanel>
        <ListView Name="ElevationQuotationTextsListView"
                  Margin="5"
                  ItemsSource="{Binding QuotationTexts.ItemsSource}"
                  SelectedItem="{Binding QuotationTexts.SelectedItem}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock FontWeight="Bold">
                            <TextBlock.Text>
                                <MultiBinding StringFormat="{}{0} - {1}">
                                    <Binding Path="Type.Id" />
                                    <Binding Path="Type.Name" />
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                        <TextBlock Text="{Binding Text}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="copy offer text"
                              Command="{Binding CopyQuotationTextCommand}" />
                </ContextMenu>
            </ListView.ContextMenu>
        </ListView>
    </DockPanel>
</views:BaseWindow>
