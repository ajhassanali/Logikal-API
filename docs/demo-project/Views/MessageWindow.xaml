<views:BaseWindow x:Class="Ofcas.Lk.Api.Client.Demo.Views.MessageWindow"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                  xmlns:utils="clr-namespace:Ofcas.Lk.Api.Client.Demo.Utils"
                  xmlns:views="clr-namespace:Ofcas.Lk.Api.Client.Demo.Views"
                  utils:DialogCloser.DialogResult="{Binding DialogResult.Value}"
                  mc:Ignorable="d"
                  WindowStartupLocation="CenterScreen"
                  ResizeMode="NoResize"
                  Title="{Binding MessageDialogTitle}"
                  SizeToContent="WidthAndHeight"
                  MinWidth="600">
    <Window.InputBindings>
        <KeyBinding Key="F7"
                    Modifiers="Alt"
                    Command="{Binding ShowDetailsCommand}" />
    </Window.InputBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <ScrollViewer MaxWidth="500"
                      MaxHeight="300"
                      VerticalScrollBarVisibility="Auto">
            <TextBox Background="Transparent"
                   BorderThickness="0"
                   FontSize="13"
                   Padding="20"
                   IsReadOnly="True"
                   Text="{Binding Message, Mode=OneWay}"
                   TextWrapping="Wrap" />
        </ScrollViewer>

        <DockPanel Grid.Row="1" Height="35">
            <DockPanel.Resources>
                <Style TargetType="{x:Type Button}">
                    <Setter Property="Width" Value="120" />
                    <Setter Property="Margin" Value="5" />
                </Style>
                <utils:NullToCollapsedConverter x:Key="NullToCollapsed" />
            </DockPanel.Resources>
            <Button DockPanel.Dock="Left" Content="Details" Visibility="{Binding ShowDetailsCommand, Converter={StaticResource NullToCollapsed}}" Command="{Binding ShowDetailsCommand}" />
            <Button DockPanel.Dock="Left" Content="Send Status Report" Visibility="{Binding SendStatusReportCommand, Converter={StaticResource NullToCollapsed}}" Command="{Binding SendStatusReportCommand}" />
            <Button DockPanel.Dock="Right" Content="OK" HorizontalAlignment="Right" IsDefault="True" IsCancel="True" />
        </DockPanel>
    </Grid>
</views:BaseWindow>
